<table class="<%= resource.class.to_s.downcase %> detail">
<% [:name, :address, :city, :state, :postal_code, :phone].each do |attr| %>
  <tr>
    <th><%= attr.to_s.titleize %></th>
    <td><%= resource.attributes[attr.to_s] %></td>
  </tr>
<% end %>
</table>